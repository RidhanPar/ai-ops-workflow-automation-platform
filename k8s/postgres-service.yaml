apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: ai-ops
  labels:
    app: postgres
    app.kubernetes.io/part-of: ai-ops-platform
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
