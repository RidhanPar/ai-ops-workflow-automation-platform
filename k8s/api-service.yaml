apiVersion: v1
kind: Service
metadata:
  name: ai-ops-api-service
  namespace: ai-ops
  labels:
    app: ai-ops-api
    app.kubernetes.io/part-of: ai-ops-platform
spec:
  selector:
    app: ai-ops-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
